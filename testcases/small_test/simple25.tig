let
    var a := 3
    function foo (): int =
    let
        var b := 0
    in
        (while b >= 0 & a > 0 do
            (b := b + a; a := a - 1)
        b
        )
    end
in
    print(chr(foo()+ord("0")))
end
