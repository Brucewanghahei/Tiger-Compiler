let
    var a := 2
    var b := 3
    function f(i:int) =
    let
    function foo (i:int) = i
    in
    if i > 0 then print(">0")
        else if i < 0 then print("<0")
            else print("=0")
    end
in
    f(0)
end
